% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mobaco.R
\name{mobaco}
\alias{mobaco}
\title{Multi-Objective Balance Optimization}
\usage{
mobaco(
  data,
  treatment_var = NULL,
  treatment_level = NULL,
  covariates = NULL,
  n_bins = 5,
  popsize = 200,
  generations = 500,
  crossover_prob = 0.8,
  mutation_prob = 0.1,
  crossover_dist = 10,
  mutation_dist = 10,
  min_per_group = NULL,
  normalization = "asymmetric",
  run_psm = FALSE,
  psm_caliper = 0.2,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing the dataset}

\item{treatment_var}{Character. Name of the treatment/group variable.
If NULL (default), will attempt auto-detection.}

\item{treatment_level}{Character/numeric. Value indicating treatment group.
If NULL (default), will use smaller group or auto-detect based on name.
The treatment group is the group of interest (e.g., "poor" prognosis, "treated", etc.)}

\item{covariates}{Character vector. Names of covariates to balance.
If NULL (default), uses all variables except treatment_var and IDs.}

\item{n_bins}{Integer. Number of bins for continuous variables (default: 5)}

\item{popsize}{Integer. NSGA-II population size (default: 200)}

\item{generations}{Integer. Number of generations (default: 500)}

\item{crossover_prob}{Numeric. Crossover probability (default: 0.8)}

\item{mutation_prob}{Numeric. Mutation probability (default: 0.1)}

\item{crossover_dist}{Integer. Crossover distribution index (default: 10)}

\item{mutation_dist}{Integer. Mutation distribution index (default: 10)}

\item{min_per_group}{Integer. Minimum patients per group (default: 5\% of smaller group)}

\item{normalization}{Character. Balance normalization: \code{"asymmetric"}
(default) divides by max(N_T, 1) as in Nikolaev et al. (2013);
\code{"symmetric"} divides by max(N_T + N_C, 1). The published results
use \code{"asymmetric"}.}

\item{run_psm}{Logical. Run PSM (MatchIt) for comparison? (default: FALSE)}

\item{psm_caliper}{Numeric. PSM caliper = caliper × SD(PS) (default: 0.2)}

\item{seed}{Integer. Random seed (default: 42)}

\item{verbose}{Logical. Print progress? (default: TRUE)}
}
\value{
A mobaco object containing:
\itemize{
\item \code{result}: NSGA-II result object
\item \code{pareto_front}: Data frame with Balance (BOSS-B) and Coverage
\item \code{best_solutions}: Indices for min_balance, knee_point, max_coverage
\item \code{selected_data}: Selected datasets for each solution
\item \code{original_data}: Original input data
\item \code{psm_data}: PSM matched data (if run_psm = TRUE)
}
}
\description{
Optimize covariate balance between treatment and control groups using
BOSS-B algorithm (Nikolaev et al. 2013) with NSGA-II genetic algorithm.
}
\examples{
\dontrun{
# Minimal usage
result <- mobaco(data = mydata)

# With PSM comparison
result <- mobaco(data = mydata, run_psm = TRUE)

# Full control
result <- mobaco(
  data = mydata,
  treatment_var = "group",
  treatment_level = "treatment",
  popsize = 500,
  generations = 10000,
  crossover_prob = 0.9,
  mutation_prob = 0.05,
  run_psm = TRUE
)
}

}
\references{
Nikolaev, A. G., Jacobson, S. H., Cho, W. K. T., Sauppe, J. J., &
Sewell, E. C. (2013). Balance optimization subset selection (BOSS):
An alternative approach for causal inference with observational data.
Operations Research, 61(2), 398-412.
https://doi.org/10.1287/opre.1120.1118
}
